# day1_cumulative_sum.py

def solve():
    # N: 日数, Q: 質問の数
    N = 5
    Q = 3
    # A: 各日の売上 (1日目〜5日目)
    A = [100, 200, 150, 300, 250]
    
    # 質問: (L日目, R日目)
    queries = [
        (1, 3), # 1日目から3日目 (期待値: 450)
        (2, 4), # 2日目から4日目 (期待値: 650)
        (1, 5)  # 1日目から5日目 (期待値: 1000)
    ]

    # 1. 累積和配列 S を作成する（S[i] は 最初のi日間の合計）
    # S = [0, 100, 300, 450, 750, 1000] になるようにする
    S = [0] * (N + 1)
    for i in range(N):
        # TODO: S[i+1] を計算するコードを1行で書く
        pass 

    # 2. 各クエリに対して、累積和を使って O(1) で答える
    for L, R in queries:
        # TODO: S を使って、L日目からR日目までの合計 ans を計算する
        ans = 0 
        print(f"{L}日目から{R}日目の売上: {ans}")

if __name__ == "__main__":
    solve()